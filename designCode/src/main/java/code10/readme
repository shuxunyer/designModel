接口之定义方法，不定义实现，也就是说每个下蛋的鸟都需要实现一遍layEgg()方法，
并且逻辑是一样的。这就会导致代码的重复，

此时我们可以针对这些接口再定义三个实现类，它们分别实现了对应接口的方法
然后通过组合和委托来消除代码的重复

我们知道继承有三个特性：
表示is-a关系、支持多态、代码复用
而这三个作用都可以通过其他技术手段来达成。
is-a关系，通过组合和接口的has-a关系来替代；多态通过接口来实现；代码复用通过组合和委托来实现。

从理论上来讲，通过接口、组合和委托三个技术手段，我们完全可以替换掉继承


